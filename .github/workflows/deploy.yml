name: Deploy on push to main

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger deployment
        run: |
          curl -f -X POST "${{ secrets.DEPLOY_WEBHOOK_URL }}/hooks/deploy-multi-tool-rust"
